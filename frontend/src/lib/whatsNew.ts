/** 「本版新增」弹窗的内容：只在**升级后**首次启动显示一次。
 *
 * 每次发版把 `version` 改成新版本号并重写两段文案即可（版本号本身来自后端
 * `/api/health` 的 `version`，不在前端另存一份，避免与 VERSION 漂移）。
 * `version` 与后端上报版本不一致时弹窗不显示——宁可不弹，也不要显示上一版的说明。
 *
 * `preserved` 是升级最容易让人紧张的部分（P3.3 配置保留确认）：明确写出升级不会碰的
 * 东西，比"升级成功"四个字有用得多。
 */
export type WhatsNew = {
  version: string;
  headline: string;
  highlights: string[];
  preserved: string[];
};

export const WHATS_NEW: WhatsNew = {
  version: "0.3.0",
  headline: "这一版让你不再错过新版本：应用内就能看到当前版本、检查更新并打开对应平台的下载页。",
  highlights: [
    "设置 → 关于：显示当前版本、最近检查时间和更新状态，手动「检查更新」随时可点。",
    "启动时静默检查一次新版本，失败只在「关于」里提示，不打扰正在做的事。",
    "发现新版本后可直接查看发布说明，并打开与本机平台/架构匹配的下载页。",
    "发布包新增 SHA-256 校验文件，下载后可自行核对完整性。",
    "AI「测试连接」发请求前会先说明费用与将使用的 Provider，成功后显示实际命中的那张卡。"
  ],
  preserved: [
    "个人画像、评分权重和岗位状态都不会丢失。",
    "`.env` 里的 AI Key 不会被覆盖或读走，界面上永远只显示「已配置 / 未配置」。",
    "自动驾驶不会被升级过程擅自打开，仍是你上次设定的模式。",
    "已排除、已跳过的岗位不会被恢复，个人看板不会被自动移动。",
    "本版没有数据库迁移，SQLite 与个人上下文仓库原样保留。"
  ]
};
